# Configurações do Servidor
# Valores possíveis: development, production
ENV=development
LOG_LEVEL=info

# Banco de dados da aplicação (SQLite) — única persistência do SAA.
# Cada alocação é um cenário autocontido; o histórico de versões vive aqui.
SQLITE_DSN=sqlite+aiosqlite:///saa.db
